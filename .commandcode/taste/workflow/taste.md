# Workflow
- Use `.\\gradlew.bat` (not `./gradlew`) for Gradle commands on this Windows project. Confidence: 0.85
- Use `--offline --no-daemon` flags with Gradle commands for this project. Confidence: 0.70
- Set `JAVA_HOME` and `PATH` explicitly per session before running Gradle commands. Confidence: 0.70
- Run `git diff --check` on changed files before marking validation as complete. Confidence: 0.70
- Before editing files that are already dirty (have uncommitted changes from prior work), preserve the current state first: `git diff > {descriptive-name}-backup.patch`, then `git status --short` to confirm what's dirty, `git diff --check` for whitespace, and compile-check — all before making any new edits. This ensures the prior dirty state is recoverable and the baseline is known-good. Confidence: 0.80
- Before implementing a UI redesign or major refactor, keep a backup copy of the previous working version (e.g., `copy <file> <file>.v{N}-backup`) so the user can compare old vs new and revert if they don't like the change. Confidence: 0.80
- Run Gradle builds as background processes (`Start-Process -WindowStyle Hidden`) with redirected stdout/stderr to log files rather than inline, then poll the logs for completion. Confidence: 0.80
- When an operation (emulator boot, build, etc.) fails repeatedly, stop and report the exact error instead of retrying indefinitely. Confidence: 0.80
- Structure multi-issue work sequentially — complete each problem fully (with visual/functional verification) before moving to the next, and use the explicit end-message the user requested. Confidence: 0.80
- Before making any code changes, first inspect and audit the current project state (git status, diffs, what's complete vs. incomplete) and report findings. Wait for explicit user approval before modifying anything. Confidence: 0.95
- When the user reports a bug or issue (especially vague or UI-glitch reports like freezing animations), do not jump to diagnosis and patch — first ask clarifying questions to scope the problem: exact location, when it started, what triggers it, and whether it's system-wide or localized. Understand the problem thoroughly before touching any code. Confidence: 0.70

- When a user reports missing capabilities on a permission-gated screen ("I can't edit/delete", "I can't input marks", "no edit option"), first verify which role/account is currently logged in (e.g., via a UI dump showing the active user's name) before treating it purely as a code bug — a role mismatch (staff logged in instead of owner) is a distinct cause where owner-only actions are correctly hidden from the current role, and the resolution may be logging in as the right account rather than a code change. Confidence: 0.70

- When the user reports a crash or bug and explicitly asks for the root cause ("root cause ber kore janao"), they want a clear causal explanation of WHY it happened — not just a patch. The final response should clearly separate Root Cause from Fix, list all contributing factors (even if multiple), and explain the mechanism of failure before showing what was changed. Confidence: 0.80
- For architectural or multi-file changes, present a structured pre-implementation plan before touching any code. The plan must include: the exact data model, the files/functions that will change, security/rules design, backward compatibility with existing data, and how duplicate/malformed state will be prevented. Do not start editing until the user approves the plan. Keep the plan short and concise — the user explicitly prefers "a short implementation plan only" over exhaustive multi-page plans. Confidence: 0.90
- Do not automatically reset, undo, restore, refactor, or clean anything when the app is working correctly — only make the changes the user explicitly requests. Confidence: 0.85

- When dead-code cleanup or cosmetic refactoring carries risk of breaking syntax/nesting/working functionality, prefer marking it as "DEFERRED — cosmetic only" rather than attempting it. Risky low-value cleanup is not worth introducing new regressions, especially when many files are already changed in the session. Confidence: 0.80
- Do not add UI elements, features, or information displays that the user did not explicitly request — even if they seem useful or related to the current task. Stick strictly to what was asked. Confidence: 0.85

- When fixing a bug, grep the codebase for all callers/consumers exhibiting the same anti-pattern and fix them in the same pass rather than addressing only the reported symptom location. Confidence: 0.80

- When reverting changes, use granular `git checkout <commit> -- <file>` to restore individual files rather than `git reset --hard` or other wholesale project-level operations — this preserves unrelated work across other problem areas intact. Confidence: 0.85

- When auditing code changes, categorize diff hunks by their origin/purpose (e.g., which problem fix, which optimization attempt) rather than just listing them — present a forensic breakdown with clear attribution so the user can distinguish completed work from unfinished or misclassified edits. Confidence: 0.80

- When new APIs or methods are introduced in a diff, trace all call sites (grep/search) to verify they are actually wired to consumers — explicitly flag dead code that has zero external callers. Confidence: 0.80

- After an audit, present actionable options with pros/cons rather than a single recommendation — let the user choose the path forward. Confidence: 0.75

- When implementing data-loading screens, use cache-first architecture: start local Room/DB observation immediately so cached data renders without delay, then launch a narrow scoped background sync (not a full-institute refresh). Never block the UI collector on a network sync. Confidence: 0.80

- Exclude non-product artifacts from Git commits — keep them on disk but never stage them. This includes: `.commandcode/taste/**`, screenshots, videos, emulator captures, build logs, and temporary/debug files. Confidence: 0.80

- For performance optimization work, scope changes to one screen at a time — fully validate the current screen and stop; do not cascade optimizations to other screens without explicit approval. Confidence: 0.80

- End task reports with a structured status line: either "[FEATURE] READY FOR VISUAL APPROVAL" (next step is user visual review) or "USER ACTION REQUIRED" (intervention needed before proceeding). Confidence: 0.80

- When fixing missing/incomplete data, use an idempotent fill-gaps approach: create only what's missing, never modify or delete existing records. Existing data safety is paramount — even when auto-generating corrections. Confidence: 0.75
- When validating correctness, verify actual database rows (before/after) rather than relying solely on expected behavior reasoning. Report concrete row counts, IDs, and states for representative scenarios. Confidence: 0.80
- Do not claim concurrency safety unless it has been tested with at least two simultaneous or repeated invocations producing the expected single-row result. Design-level arguments alone are insufficient to assert thread-safety. Confidence: 0.80

- When conducting a correctness audit, trace every code path exhaustively — read all call sites, entity models, DAO queries, seeder/format helpers, and screen-level consumers rather than inferring behavior from a single file. Include direct code snippets or line references as evidence for each finding. Confidence: 0.80

- Structure code audits with numbered risk sections, each containing: a clear statement of the risk, current-behavior analysis with code evidence, and an explicit ✅/❌ verdict. End the audit with a summary table plus a binary overall conclusion (e.g. "PROBLEM 05 AUDIT PASSED" or "PROBLEM 05 NEEDS CORRECTION"). Confidence: 0.75
- For comprehensive module/screen audits (covering an entire file or feature area), categorize findings by severity tier with emoji markers: 🔴 Critical, 🟠 High, 🟡 Medium, 🟢 Low. Number findings within each tier, include line-number references as code evidence, and end with a summary table. After presenting the audit, offer to begin fixes in severity-priority order (critical → high → medium → low). Confidence: 0.85

- Before production launch, conduct a comprehensive full-app audit covering ALL features/modules — not just a single module or screen. The audit should be exhaustive and careful: identify bugs, polish gaps, and improvement opportunities across the entire codebase. Present findings in severity tiers with a summary table, then include a short "Top N to fix before production" prioritized list. The user explicitly scopes this phase as "audit only" — do not start fixing until explicitly asked. Confidence: 0.80
- Before modifying any code that touches existing calculation or business-logic paths, first trace the runtime data flow end-to-end — read all call sites, state variables, callbacks, and downstream consumers — and confirm understanding of the current behavior. Do not make changes until the existing path is fully understood. Confidence: 0.85
- When implementing a feature that overlaps with existing business rules (payment allocation, discount logic, fee calculation, admission-date constraints, etc.), trace and preserve the current behavior rather than inventing new rules. If the existing behavior is unclear or insufficient, stop and ask the user rather than guessing. Confidence: 0.80
- When a requested change would require modifying areas explicitly scoped as out-of-bounds for the current phase (or explicitly listed as "Do NOT touch"), stop and report the conflict for a later phase rather than creeping scope — even if the change seems small or justified. Confidence: 0.85
- When you cannot access or verify a resource (e.g., a Firebase project with 403/INSUFFICIENT_PERMISSION), explicitly qualify every conclusion — state what you cannot determine due to the access gap rather than making definitive claims about the inaccessible resource. Never assert facts about something you were blocked from inspecting. Confidence: 0.85
- When the user asks for evidence or verification, provide exact forensic references: file paths, line numbers, raw `git diff` output, commit hashes, and terminal command transcripts — not prose summaries that merely claim a fix was made. Every factual claim must be traceable to concrete output. Confidence: 0.90
- Do not invent or reference testing scenarios (e.g., "phone test passed," "tested on a real device") that the user never performed or mentioned. Only reference tests, devices, or environments the user has actually conducted or explicitly acknowledged. Confidence: 0.85
- When the user specifies a particular verification/inspection method (e.g., "use FirebaseAuth.currentUser, not shared prefs"), use that exact method — don't silently substitute an indirect proxy and present its results as equivalent. The user chose the method for a reason. Confidence: 0.85
- When the user issues an explicit "do not make any edits, deploys, exports, imports, migrations, reinstalls, or deletions" directive and says "wait for my approval," enter a strict read-only mode: only inspect, audit, and report. Treat this as extending to all mutating actions — tests, deploys, exports, and data changes — not just source-code edits. Confidence: 0.85
- When the user asks for a verification/test plan, present a structured checklist with checkboxes organized by feature area and wait for explicit approval before executing any of it — never run tests, even read-only ones, without the user's go-ahead. Confidence: 0.75
- When asked whether a feature is working or what it depends on, decompose the answer into orthogonal concern categories (e.g., source code, Firestore data, security rules, Auth role, hosting config) and give a clear yes/no/explanation for each — don't give a single monolithic answer that conflates them. Confidence: 0.75
- Commit and push to Git as a backup/savepoint before transitioning to a new phase or major feature area — the user treats pushes as checkpoints they can return to if needed. Confidence: 0.80
- When implementing features that involve amounts/prices/fees, make them configurable by the end user (institute owner) rather than hardcoded or static. Support standard business operations like editable amounts, discounts, and partial payments — don't ship a minimal/hardcoded version that lacks real-world flexibility. Confidence: 0.70
- Push all changes to Git before ending a working session — the user treats pushed commits as savepoints so they can `git pull` and resume from exactly where they left off later. Confidence: 0.85
- When closing out a working session, provide a structured summary (table or checklist) of every completed feature so the user can mentally checkpoint what was done before stepping away. Confidence: 0.80

- Never deliver a "FINAL REPORT," completion summary, or production verdict until ALL planned work is genuinely finished — premature finalization (stopping mid-stream to present a report claiming completion) is treated as a serious workflow violation, not an acceptable interim checkpoint. The final report must only come when there is no remaining work to do. Confidence: 0.90

- When working through a numbered audit/issue list (e.g., #1–#41), every single item must appear in the final status table with an explicit verdict — FIXED, FALSE ALARM, DEFERRED, or NOT VERIFIED. Silently omitting items is not acceptable. False alarms must include evidence/justification for the dismissal, not just be skipped or dropped. Confidence: 0.90

- Build the APK and verify installation (`assembleDebug` + `adb install -r`) after each logically-related group of changes during autonomous multi-phase execution, not just once at the very end. Final verification must include a clean build + APK install. Confidence: 0.75

- Only declare "PRODUCTION READY" when zero unresolved Critical or High production blockers remain. If any Critical or High items are still NOT VERIFIED or unresolved, the verdict must honestly reflect that — do not claim readiness while blockers exist. Confidence: 0.85
- When the user asks for an audit "since version X" (e.g., "Version 1.4-এর পর থেকে"), use git tags or the corresponding merge-commit hash as explicit range boundaries (`tag..HEAD`) for scoping the audit — the user treats version tags as milestone markers, not ad-hoc date or file-based cutoffs. Confidence: 0.80
- When the user explicitly scopes an investigation to a specific UI area or widget and says "only observe and report on this, don't go elsewhere," stay strictly within that scope — do not branch into tangential investigations, do not make unrelated code changes, and do not use the investigation as a jumping-off point to fix other things the user didn't ask about. The user wants a focused report, not a roaming audit. Confidence: 0.75
- Prefers small, scoped tasks over large multi-feature work items. The user explicitly stated they will give shorter tasks going forward — keep each work unit narrow and focused rather than bundling many features into one session. Confidence: 0.90
- When the user selects a single numbered item from an audit report for fixing, treat everything else in the audit as out of scope — do not touch any other issues, even within the same file. Fix only that one item. Confidence: 0.95
- After completing a bug fix, provide a structured post-fix report with these specific sections: Changed File(s), Root Cause, Exact Fix, Why This Approach (rationale), Possible Side Effects, and Build/Test Result. The user explicitly wants this format rather than a free-form summary. Confidence: 0.85
- Build success alone does NOT mean an issue is fixed. Before claiming a fix is complete, perform actual functional verification: test the specific scenarios the user outlined (happy path, failure cases, edge cases, data consistency checks), and report concrete results. "It builds" is not a test result. Confidence: 0.90

- When doing final regression after multiple fixes across financial modules, trace the full end-to-end financial flow chain (fee creation → discount → payment → partial payment → due calculation → receipt generation → payment history) rather than verifying only the specific lines changed. Financial bugs often manifest downstream of the edited code, and isolated line-level verification misses propagation errors. Confidence: 0.80
- After completing a single-issue fix (especially one picked from an audit), stop and wait for the user to request the next issue — never auto-proceed to the next audit item without being explicitly asked. Confidence: 0.90

- The user can authorize an "autonomous mode" for multi-phase work: once a phase plan is reviewed and accepted, the assistant proceeds through ALL planned phases without per-phase approval, as long as each phase builds/compiles successfully before advancing to the next. Build verification is the gate between phases. This explicitly overrides the default "wait for approval" workflow preference when the user triggers autonomous mode. Confidence: 0.85
- When the user accepts a multi-phase plan and then authorizes autonomous execution, the plan review itself serves as the approval checkpoint — individual phases within the plan do not need separate sign-off. The assistant should present the complete plan first, get acceptance, then execute autonomously. Confidence: 0.80
- When working on a specific module or feature area (e.g., staff, payments, attendance), restrict all changes to that module only — do not touch code in other modules or feature areas, even if they seem related or could benefit from changes. Maintain strict module boundaries. Confidence: 0.85
- For large, complex existing files (especially screens like SuperAdminScreen.kt), use targeted/surgical edits rather than wholesale file replacement — do not overwrite the complete file. Preserve all existing working features by making only the necessary insertions and replacements. Confidence: 0.85
- Before implementing major feature additions to an existing screen/panel, produce a structured audit/plan document (modules, priority, effort, impact ratings) that covers both user-requested features AND proactively-suggested improvements — the user values developer insight on what else could be done, not just a passive implementation of stated requests. Confidence: 0.75
- When verifying which Firebase project an Android app is bound to, treat `google-services.json` (SDK-level project binding) and `.firebaserc` (CLI/tools-level project binding) as the two canonical proof files — if both point to the same project, the binding is confirmed; these are the user's definitive config artifacts for project identity. Confidence: 0.80
- Git commit messages should use a structured format: conventional commit type prefix (e.g., `fix:`, `feat:`), a short summary line, blank line, then categorized sections with bullet points listing individual changes. Confidence: 0.75
- On Windows, use `git commit -F <tempfile>` for multi-line commit messages — POSIX heredocs (`<<'EOF'`) don't work in PowerShell/CMD. Write the message to a temp file (e.g., `commit_msg.txt`) then commit with `-F`. Confidence: 0.90
- Prefer manual trigger for outbound communications (WhatsApp, SMS, email) rather than auto-sending upon approval — the admin should explicitly choose to send, and the document/receipt should remain accessible even if they skip sending. Auto-send creates problems when the messaging app isn't installed or when the admin wants to review before sharing. Confidence: 0.90
- When the user asks a variant of "can we see it in the emulator?" or "will it work?", prefer to build the APK (`assembleDebug`), install it on the emulator (`adb install -r`), launch the app, and capture a screenshot — the user values concrete visual proof over verbal assurances. Confidence: 0.75
- Prefers to personally test the app on the emulator to discover problems themselves before asking the assistant to fix anything — the user does their own QA/issue-discovery pass on the emulator first, then reports what needs fixing. The user will explicitly ask to test things themselves ("let me login and see the student part") rather than passively accepting automated demos or screenshots. Confidence: 0.90

- When a fix involves editing Firebase/Firestore security rules (firestore.rules), always deploy them (`npx firebase deploy --only firestore:rules`) in the same pass — local rule edits have no effect until deployed, and skipping deployment wastes the user's testing cycle with a still-broken state. Confidence: 0.80

- When given a choice between a quick demo/mock approach (e.g., demo login, auto-created test data) and a real production flow (e.g., creating Firebase Auth accounts, setting up real data), prefers to test with the real production flow — the user explicitly stopped a demo login feature mid-implementation and asked to revert it, opting to manually create real credentials through the admin flow instead. Values authentic end-to-end verification over shortcuts. Confidence: 0.85

- Receipt generation and sharing (PDF + WhatsApp) should be consistent across all payment-type modules — salary payment flows must produce shareable receipts just like student fee collection flows. Receipt generation is treated as a core, non-optional part of any payment workflow, not a separate feature. Confidence: 0.70
- Payment receipts (salary, fee collection, etc.) must be persistently re-accessible after the initial payment — not a one-time dialog that disappears once dismissed. Paid records should carry a "Receipt" button so the admin can re-print or re-share the receipt at any time later. Confidence: 0.85
- Do not touch the app's UI, layout, theme, or color combination unless the user explicitly instructs a specific visual change (e.g., "change this screen's this color"). Even while working on a related feature, leave all UI/color styling untouched. Confidence: 0.85
- When working on next-release features (e.g., "version 1.7"), keep that work clearly separated from what is live in production — the user has a live Play Store app plus deployed Firebase rules/functions that real users depend on, and local rule/function changes must not break them. Before treating changes as safe, verify the production deploy state (git log/deploy logs for `firestore.rules` and functions) so newly added rules/functions aren't already live against the old app. Confidence: 0.85

- After implementing a scoped feature, end with a list of modified files and explicitly confirm the changes were limited to the requested feature's scope — the user wants to verify no other module was touched before accepting the work. Confidence: 0.75
