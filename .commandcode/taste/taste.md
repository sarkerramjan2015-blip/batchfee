# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# Workflow
- Use `.\\gradlew.bat` (not `./gradlew`) for Gradle commands on this Windows project. Confidence: 0.70

# Communication
- Respond in Bengali when the user speaks Bengali. Confidence: 0.75

# Workflow
- Only build APK (`assembleRelease`) when the user explicitly asks for it. Do not auto-build after every code change. Confidence: 0.85

