fix: superadmin orphan cleanup, revenue stats, remove button, password reset

SuperAdmin:
- Fix permanentlyDelete: clean all 10 subcollections (batches, students,
  fees, payments, receipts, attendance, exams+results, staffs, enquiries,
  registrations)
- Fix recalculateStats: check expired/trashed/blocked status for accurate
  revenue and active count
- Fix removeInstitute: use Firebase Auth reauthentication instead of
  Room DB password hash verification (backfill had empty hashes)
- Fix sendPasswordReset: migrate to coroutine with Tasks.await()

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
