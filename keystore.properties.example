# This file is safe to commit only because it contains placeholders. Copy it to the ignored
# keystore.properties file for local use, or supply the same release* properties from CI secrets.
# Paths are resolved from the repository root; absolute paths are also supported.
releaseStoreFile=app/batchfee-release.jks
releaseStorePassword=your-store-password
releaseKeyAlias=your-key-alias
releaseKeyPassword=your-key-password
